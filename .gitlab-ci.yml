stages:
  - test
  - build

build:
  tags:
    - ubuntu
  stage: build
  script:
    - echo "cat /etc/os-release"
    - echo "going to compile"
    #- make build

test:
  tags:
    - ubuntu
  stage: test
  script:
    - echo "going to test"